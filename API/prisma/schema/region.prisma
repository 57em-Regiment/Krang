model Region {
  id            String @id @default(uuid())
  name          String @unique

  gameRegionId  Int?

  towns         Town[]
  locations     Location[]

  @@index([name(ops: raw("gin_trgm_ops"))], type: Gin)
}
